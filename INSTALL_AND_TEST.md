# 🚀 SoulMap 安裝與測試指南

## ✅ 前置檢查

在開始之前，確認你已安裝：
- ✅ Node.js (v18 或更高版本)
- ✅ npm (v9 或更高版本)

檢查版本：
```bash
node --version   # 應該 >= v18.0.0
npm --version    # 應該 >= 9.0.0
```

如果沒有安裝，請前往 [Node.js 官網](https://nodejs.org/) 下載安裝。

---

## 📦 步驟 1: 安裝依賴

```bash
cd soulmap
npm install
```

**預期結果**：
- ✅ 終端機顯示安裝進度
- ✅ 生成 `node_modules` 資料夾
- ✅ 生成 `package-lock.json` 檔案
- ✅ 沒有錯誤訊息（warnings 可以忽略）

**如果遇到錯誤**：
```bash
# 清除快取重試
npm cache clean --force
npm install
```

---

## 🔥 步驟 2: 啟動開發伺服器

```bash
npm run dev
```

**預期結果**：
```
   ▲ Next.js 14.x.x
   - Local:        http://localhost:3000
   - Environments: .env.local

 ✓ Ready in Xs
```

**如果 3000 port 被佔用**：
```bash
# Windows
netstat -ano | findstr :3000
# 找到 PID 後關閉該程序

# 或使用其他 port
npm run dev -- -p 3001
```

---

## 🧪 步驟 3: 功能測試清單

### 測試 1: 首頁載入 ✅

1. 開啟瀏覽器訪問 `http://localhost:3000`
2. 應該看到：
   - ✅ Hero 區塊：「星光懂你沒說的話」
   - ✅ 熱門心理測驗區（1 張測驗卡片）
   - ✅ 12 星座預覽卡片
   - ✅ 靈魂配對入口

**截圖檢查點**：
- 背景是深藍色 (#0B0F2F)
- 標題是大字體、白色
- 按鈕有紫色漸層效果
- 頁首有 Logo 和導覽列

---

### 測試 2: 測驗流程 ✅

1. 點擊「開始測驗」或前往 `/tests`
2. 看到「情感依附型態測驗」卡片
3. 點擊「開始測驗」
4. 應該看到：
   - ✅ 進度條（第 1 題 / 共 10 題）
   - ✅ 題目文字
   - ✅ 5 個選項（非常不同意 ~ 非常同意）
   - ✅ 上一題/下一題按鈕
5. 選擇任一選項
6. 點擊「下一題」
7. 重複直到最後一題
8. 點擊「看結果」
9. 應該看到：
   - ✅ 結果標籤（例如「安全依附者 🌟」）
   - ✅ 摘要說明
   - ✅ 優勢列表
   - ✅ 盲點列表
   - ✅ 建議文字
   - ✅ 廣告位 placeholder
   - ✅ 「解鎖完整解析」按鈕
   - ✅ 「分享結果」按鈕

**測試不同答案**：
- 全選「非常同意」→ 應該得到「焦慮依附者」
- 全選「非常不同意」→ 應該得到「迴避依附者」
- 平均分配 → 應該得到「安全依附者」或「矛盾依附者」

---

### 測試 3: 星座運勢 ✅

1. 點擊導覽列的「星座」或前往 `/astro`
2. 看到 12 個星座卡片
3. 點擊任一星座（例如「牡羊座」）
4. 應該看到：
   - ✅ 大型星座 emoji 和名稱
   - ✅ 元素標籤（火/土/風/水）
   - ✅ 今日運勢（愛情/工作/心情/建議）
   - ✅ 本週運勢（愛情/工作/心情/建議）
   - ✅ 本月運勢（愛情/工作/心情/建議）
   - ✅ 廣告位 placeholder
   - ✅ 返回所有星座的連結

**測試多個星座**：
- 每個星座都有不同的內容
- URL 格式：`/astro/aries`, `/astro/taurus` 等

---

### 測試 4: 配對分析 ✅

1. 點擊導覽列的「配對」或前往 `/match`
2. 填寫表單：
   - 對象 A：選「獅子座」
   - 對象 B：選「水瓶座」
   - 關係狀態：選「曖昧中」
3. 點擊「分析配對」
4. 應該看到：
   - ✅ 合拍度分數（例如 75%）
   - ✅ 相處模式描述
   - ✅ 容易吵架的點
   - ✅ 溝通建議

**測試不同組合**：
- 同元素（火+火）：高分
- 互補元素（火+風）：中高分
- 衝突元素（火+水）：低分
- 特殊配對（獅子+水瓶）：有客製化描述

---

### 測試 5: 靜態頁面 ✅

1. **關於我們** (`/about`)
   - ✅ 看到 SoulMap 和 StarMuse 的介紹
   - ✅ 使命說明
   - ✅ 免責提醒

2. **聯絡我們** (`/contact`)
   - ✅ 表單有姓名、Email、主旨、訊息欄位
   - ✅ 填寫並送出（會跳出 alert）
   - ✅ 表單清空

3. **隱私政策** (`/legal/privacy`)
   - ✅ 看到完整的隱私政策內容

4. **免責聲明** (`/legal/disclaimer`)
   - ✅ 看到完整的免責聲明
   - ✅ 有專業協助資源連結

---

### 測試 6: 響應式設計 ✅

1. 開啟瀏覽器開發者工具 (F12)
2. 切換到裝置模擬模式
3. 測試不同尺寸：
   - **手機** (375px):
     - ✅ 導覽列變成摺疊式
     - ✅ 卡片變成單欄
   - **平板** (768px):
     - ✅ 卡片變成雙欄
   - **桌面** (1024px+):
     - ✅ 卡片 3-4 欄
     - ✅ 完整導覽列

---

### 測試 7: 互動功能 ✅

1. **深色模式切換**
   - 點擊頁首的 🌙/☀️ 按鈕
   - (目前只是切換 emoji，未來可整合真實功能)

2. **測驗導覽**
   - 測驗中點「上一題」應該回到上一題
   - 未回答時「下一題」應該是 disabled
   - 最後一題應該顯示「看結果」而非「下一題」

3. **結果頁面按鈕**
   - 點擊「解鎖完整解析」→ alert
   - 點擊「分享結果」→ alert

---

## 🎯 效能檢查

### 建置測試

```bash
npm run build
```

**預期結果**：
- ✅ 成功編譯所有頁面
- ✅ 顯示各頁面大小
- ✅ 沒有 TypeScript 錯誤
- ✅ 沒有 ESLint 錯誤（warnings 可忽略）

**如果編譯失敗**：
- 檢查 TypeScript 錯誤訊息
- 確認所有 import 路徑正確
- 檢查是否有語法錯誤

### 啟動生產版本

```bash
npm start
```

訪問 `http://localhost:3000`，應該與開發版本一樣運作正常。

---

## 🔍 常見問題排除

### Q1: npm install 一直卡住？

**解決方法**：
```bash
# 切換 npm registry
npm config set registry https://registry.npmmirror.com
npm install

# 如果還是不行，手動刪除後重試
rm -rf node_modules package-lock.json
npm install
```

### Q2: 頁面空白或顯示錯誤？

**檢查步驟**：
1. 開啟瀏覽器 Console (F12)
2. 查看是否有紅色錯誤訊息
3. 檢查終端機是否有編譯錯誤
4. 確認所有檔案都已正確建立

**常見錯誤**：
- `Module not found`: 檢查 import 路徑
- `Unexpected token`: 檢查 TypeScript 語法
- `Cannot find module '@/...'`: 檢查 tsconfig.json 的 paths 設定

### Q3: 樣式跑掉或沒有套用？

**解決方法**：
```bash
# 清除 .next 快取
rm -rf .next
npm run dev
```

或檢查：
- `app/globals.css` 是否正確引入
- `tailwind.config.ts` 設定是否正確
- 瀏覽器是否快取舊版本（強制重新整理 Ctrl+Shift+R）

### Q4: 測驗結果計算錯誤？

**除錯方法**：
在 `lib/quizzes.ts` 的 `scoreToResult` 函式中加入：
```typescript
scoreToResult: (scores, extra) => {
  console.log('收到的分數:', scores);  // 👈 加這行
  // ... 其他程式碼
}
```

重新執行測驗，在瀏覽器 Console 中查看實際分數。

---

## ✅ 測試完成檢查表

完成以上所有測試後，勾選以下項目：

- [ ] 首頁正常顯示
- [ ] 測驗流程完整（列表→測驗→結果）
- [ ] 星座運勢正常（總覽→詳細頁）
- [ ] 配對分析正常運作
- [ ] 所有靜態頁面可訪問
- [ ] 手機版響應式正常
- [ ] 建置成功無錯誤
- [ ] 生產版本正常運作

---

## 🎉 全部測試通過！

恭喜！你的 SoulMap 專案已經完全就緒！

### 下一步：

1. **客製化內容**
   - 參考 `QUICK_START.md` 新增測驗
   - 更新星座運勢文案
   - 調整配色與樣式

2. **部署到網路**
   - 建立 GitHub 儲存庫
   - 推送程式碼
   - 連結 Vercel 部署
   - 獲得公開網址

3. **持續開發**
   - 新增更多測驗類型
   - 整合使用者系統
   - 串接真實星座 API
   - 添加分享功能

---

**需要協助？**
- 📖 查看 `README.md` 完整文件
- 🚀 參考 `QUICK_START.md` 快速任務
- 🏗️ 閱讀 `PROJECT_STRUCTURE.md` 了解架構

**祝你順利！✨💜**
